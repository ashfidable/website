---
import type { HTMLAttributes } from 'astro/types'
interface Props extends HTMLAttributes<'section'> {}
const { ...attr } = Astro.props
---

<section {...attr} class="flex flex-col gap-6">
	<slot />
</section>

<style>
	section > section:not(:last-child) {
		@apply mb-6;
	}
</style>
